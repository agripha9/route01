-- ============================================================================
-- Route01 — Migration 004: rename funding column to invest
-- 작성일: 2026-04-29
--
-- 배경: 003에서 'funding' 이름으로 추가했으나, 화면 코드 전체가 'invest'
--       이름을 쓰고 있음 (ob.invest, profile.invest, pickChip('invest', ...)).
--       화면 코드를 정본으로 보고 DB 컬럼명을 invest로 통일.
-- ============================================================================

alter table public.profiles rename column funding to invest;

-- ============================================================================
-- 끝.
-- 실행 후 Table Editor에서 profiles 테이블 새로고침 → invest 컬럼 확인.
-- ============================================================================
